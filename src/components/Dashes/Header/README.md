# Dashboard Header Component 📋

## نظرة عامة
كومبوننت هيدر حديث ومنفصل لوحة التحكم مع تصميم متجاوب وكلاسات CSS مميزة ومنظمة.

## المميزات ✨
- 🎨 تصميم عصري مع تدرج لوني جذاب
- 📱 متجاوب تماماً مع جميع أحجام الشاشات
- 🍔 قائمة هامبورغر للموبايل مع أزرار الفلتر
- 🖥️ تصميم منفصل للديسكتوب والموبايل
- ♿ دعم كامل لمعايير إمكانية الوصول (a11y)
- 🎭 تأثيرات تفاعلية سلسة
- 🌓 دعم الوضع الداكن (Dark Mode)
- 🖨️ أنماط خاصة للطباعة
- ⚡ أداء محسّن مع React.memo

## الاستخدام 🔧

### استيراد الكومبوننت
```jsx
import DashboardHeader from './Header/DashboardHeader';
```

### مثال أساسي
```jsx
<DashboardHeader 
  onHomeClick={handleHome}
  onLogoutClick={handleLogout}
  onFilterSelect={handleFilterSelect}
  filterItems={filterItems}
  activeFilter={activeFilter}
/>
```

### مع دوال مخصصة
```jsx
const handleHome = () => {
  navigate('/');
  console.log('Going to home...');
};

const handleLogout = () => {
  localStorage.removeItem('userToken');
  navigate('/auth/login');
};

<DashboardHeader 
  onHomeClick={handleHome}
  onLogoutClick={handleLogout}
/>
```

## الخصائص (Props) 📝

| الخاصية | النوع | مطلوبة | الوصف |
|---------|------|--------|--------|
| onHomeClick | Function | نعم | دالة يتم استدعاؤها عند النقر على زر الرئيسية |
| onLogoutClick | Function | نعم | دالة يتم استدعاؤها عند النقر على زر تسجيل الخروج |
| onFilterSelect | Function | نعم | دالة يتم استدعاؤها عند اختيار فلتر من القائمة |
| filterItems | Array | لا | مصفوفة عناصر الفلتر للعرض في القائمة |
| activeFilter | String | لا | الفلتر النشط حالياً |

## الكلاسات CSS 🎨

### الكلاسات الرئيسية

#### الهيدر الأساسي:
- `dashboard-header-wrapper` - الحاوية الخارجية للهيدر
- `dashboard-header-content` - محتوى الهيدر
- `dashboard-header-title` - عنوان لوحة التحكم
- `dashboard-header-left` - الأزرار على اليسار (موبايل)
- `dashboard-hamburger-btn` - زر قائمة هامبورغر

#### الأزرار:
- `dashboard-nav-btn` - زر التنقل الأساسي
- `dashboard-nav-btn--home` - نمط خاص لزر الرئيسية
- `dashboard-nav-btn--back` - نمط خاص لزر العودة/تسجيل الخروج
- `dashboard-nav-icon` - أيقونة الزر

#### قائمة الموبايل:
- `dashboard-mobile-menu-overlay` - خلفية القائمة المنبثقة
- `dashboard-mobile-menu` - القائمة نفسها
- `dashboard-mobile-menu-item` - عنصر في القائمة
- `dashboard-mobile-menu-item--new-booking` - زر حجز جديد مميز
- `dashboard-mobile-menu-actions` - أزرار الإجراءات في أسفل القائمة

### التخصيص
يمكنك تخصيص الألوان والأحجام من خلال تعديل ملف `DashboardHeader.css`:

```css
.dashboard-header-wrapper {
  background: linear-gradient(135deg, #YourColor1 0%, #YourColor2 100%);
}

.dashboard-nav-btn {
  padding: /* حجمك المخصص */;
  border-radius: /* نصف قطر الزاوية */;
}
```

## التجاوب 📱

### نقاط التوقف (Breakpoints)
- **Desktop**: أكثر من 768px (يعرض التنقل الكامل)
- **Tablet**: 768px - 481px (يخفي التنقل الكامل)
- **Mobile**: 480px - 361px (يعرض أزرار مربعة)
- **Small Mobile**: 360px وأقل (أزرار أصغر)

### التصميم المتجاوب:
- **Desktop**: يعرض التنقل الكامل مع النصوص
- **Mobile**: يعرض أزرار مربعة + قائمة هامبورغر
- **الانتقال السلس**: بين التصميمين

## إمكانية الوصول ♿

الكومبوننت يتضمن:
- ✅ تسميات ARIA مناسبة
- ✅ دعم التنقل بلوحة المفاتيح
- ✅ Focus states واضحة
- ✅ دعم وضع التباين العالي
- ✅ دعم خيار تقليل الحركة

## التوافقية 🌐

- ✅ جميع المتصفحات الحديثة
- ✅ Chrome/Edge 90+
- ✅ Firefox 88+
- ✅ Safari 14+
- ✅ Opera 76+

## الأداء ⚡

- استخدام `React.memo` للتحسين (إذا لزم الأمر)
- تأثيرات CSS فعالة
- لا استخدامات مكلفة للـ re-renders
- أحجام ملفات صغيرة

## الملفات المرتبطة 📁
```
Header/
├── DashboardHeader.jsx    # الكومبوننت الرئيسي
├── DashboardHeader.css    # الأنماط
└── README.md              # هذا الملف
```

## أمثلة متقدمة 💡

### مع تأكيد قبل تسجيل الخروج
```jsx
const handleLogoutWithConfirmation = () => {
  if (window.confirm('هل أنت متأكد من تسجيل الخروج؟')) {
    handleLogout();
  }
};

<DashboardHeader 
  onHomeClick={handleHome}
  onLogoutClick={handleLogoutWithConfirmation}
/>
```

### مع تتبع الأحداث
```jsx
const handleHomeWithTracking = () => {
  analytics.track('Home Button Clicked');
  navigate('/');
};

const handleLogoutWithTracking = () => {
  analytics.track('Logout Button Clicked');
  logout();
};

<DashboardHeader 
  onHomeClick={handleHomeWithTracking}
  onLogoutClick={handleLogoutWithTracking}
/>
```

## الملاحظات 📌
- الكومبوننت يستخدم `react-icons/fa` للأيقونات
- التصميم يدعم اللغة العربية (RTL)
- جميع النصوص قابلة للتعديل

## الدعم والمساعدة 🆘
للمساعدة أو الإبلاغ عن مشاكل، يرجى التواصل مع فريق التطوير.

---
**آخر تحديث**: أكتوبر 2025
**الإصدار**: 1.0.0

